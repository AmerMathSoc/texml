LaTeX and perl have two very different inheritance models.

LaTeX: inheritance via redefining:

    \LoadClass{amsart}

    \renewcommand{\section}...

or

    \usepackage{foo}

where foo.sty has

    \renewcommand{\section}...

perl: class inheritance:

    use base qw(amsart);

    sub section { ... }
