#!/usr/bin/perl -w

use strict;
use warnings;

use lib qw(/work/ptg/src/perl/trunk);

use version; our $VERSION = qv '0.0.0';

use TeX::Interpreter::LaTeX;

my $t = TeX::Interpreter::LaTeX->new();

$t->INITIALIZE();

$t->list_macros();

__END__
