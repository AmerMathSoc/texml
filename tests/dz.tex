\documentclass{article}

\usepackage{fontspec}

\setmainfont{STIX Two Text}

\begin{document}

hello, world

% small z dot: 017C = 380
% large z dot: 017B = 379

\lccode`\a=380 % zdot

% uccode(380): \the\uccode380

% lccode(379): \the\lccode379

\lowercase{\toks0={a}}

\showthe\toks0

Zdot: \uppercase\expandafter{\the\toks0}

zdot: \the\toks0

catcode(454) = \the\catcode454

sfcode(454) = \the\sfcode454

lccode(454) = \the\lccode454

uccode(454) = \the\uccode454

tccode(454) = \the\tccode454

mathcode(454) = \the\mathcode454

delcode(454) = \the\delcode454

\uppercase{\char454}

lowercase: ǆ (titlecase = \char\tccode`\ǆ)

uppercase: Ǆ (titlecase = \char\tccode`\Ǆ)

titlecase: ǅ (titlecase = \char\tccode`\ǅ)

uppercase(ǆ) = \uppercase{ǆ}

uppercase(ǅ) = \uppercase{ǅ}

lowercase(Ǆ) = \lowercase{Ǆ}

lowercase(ǅ) = \lowercase{ǅ}

titlecase(ǆ) = \titlecase{ǆ}

titlecase(Ǆ) = \titlecase{Ǆ}

titlecase(ǅ) = \titlecase{ǅ}

\begingroup
\tccode`\ǆ=`\D

lowercase: ǆ (titlecase = \char\tccode`\ǆ)

\endgroup

lowercase: ǆ (titlecase = \char\tccode`\ǆ)

\end{document}
